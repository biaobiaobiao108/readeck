{
  "name": "readeck-bun",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "dev:server": "bun --watch apps/server/src/index.ts",
    "dev:web": "bun build ./apps/web/src/index.tsx --outdir ./apps/web/dist --target browser --watch",
    "build": "bun --filter web build",
    "start": "bun apps/server/src/index.ts",
    "test": "bun test",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
